# finds - Copyright (c) 2026 Kirizaki

target-version = "py312"
line-length = 120

[lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line too long (handled by formatter preference, not strict)
]

[lint.per-file-ignores]
"tests/*" = ["F811"]  # allow fixture redefinition in tests
